using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Collectibles : MonoBehaviour
{
    //creates an item type field to add list items(list created in playerMovemet.cs) via Unity
    //in Unity under each collectible add the name of that object
    public string itemType;
}
