using UnityEngine;
using UnityEngine.UI;

public class Score : MonoBehaviour
{
    public Text coinCount; //drag the text componet in inspector onto slot

    // Update is called once per frame
    void Update()
    {
        //transfer count onto ui element
        //coinCount.text = inventory coin amount .ToString();
    }
}
